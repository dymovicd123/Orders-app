PRAGMA foreign_keys = ON;

-- v72. Step 03 does not need new columns.
-- This file is intentionally present so the local setup has an explicit migration step marker.
SELECT 'v72 step 03 order edit + stock recalculation' AS migration_marker;
