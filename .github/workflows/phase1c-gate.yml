name: Phase 1C release gate
# Retry after fixing a test-only source-section boundary.

on:
  push:
    branches:
      - branch2
    paths:
      - .github/workflows/phase1c-gate.yml

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: branch2
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - run: npm run release:check
