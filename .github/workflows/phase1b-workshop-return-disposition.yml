name: Phase 1B Workshop return disposition patch

on:
  push:
    branches:
      - branch2
    paths:
      - .github/workflows/phase1b-workshop-return-disposition.yml

permissions:
  contents: write

jobs:
  apply:
    if: ${{ github.event.head_commit.message == 'Repair one-shot Phase 1B patch workflow' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout branch2
        uses: actions/checkout@v4
        with:
          ref: branch2
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply exact source patch
        run: python3 scripts/apply-phase1b-workshop-disposition.py

      - name: Refresh accepted Worker declaration hashes
        run: node scripts/refresh-phase1b-worker-manifest.mjs

      - name: Inspect patch
        run: |
          git diff --check
          git diff --stat
          git diff -- worker/domains/returns-exchanges.ts src/App.tsx src/app/types.ts src/app/utils.ts src/features/sections/OrderReturnsSection.tsx src/features/sections/OrderExchangeSection.tsx scripts/test-step192a1-warehouse-truth-freshness.mjs scripts/step192a1-warehouse-truth-freshness-manifest.json

      - name: Full release gate
        run: npm run release:check

      - name: Commit verified source and remove one-shot helpers
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/workflows/phase1b-workshop-return-disposition.yml scripts/apply-phase1b-workshop-disposition.py scripts/refresh-phase1b-worker-manifest.mjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add worker/domains/returns-exchanges.ts src/App.tsx src/app/types.ts src/app/utils.ts \
            src/features/sections/OrderReturnsSection.tsx src/features/sections/OrderExchangeSection.tsx \
            scripts/test-step192a1-warehouse-truth-freshness.mjs scripts/step192a1-warehouse-truth-freshness-manifest.json
          git diff --cached --quiet && { echo 'No verified source changes to commit'; exit 1; }
          git commit -m "Fix Workshop return disposition semantics"
          git push origin HEAD:branch2
