name: Apply finance day human polish

on:
  push:
    branches:
      - fix/finance-day-human-polish-20260916

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: fix/finance-day-human-polish-20260916
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply bounded polish
        run: python3 scripts/patch-finance-day-human-polish.py
      - name: Focused finance regression
        run: node scripts/test-finance-day-transparency.mjs
      - name: Typecheck
        run: npx tsc -b --pretty false
      - name: Diff check
        run: git diff --check
      - name: Commit validated polish
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/features/renderers/FinanceDashboardRenderer.tsx src/features/finance/finance-day.css scripts/test-finance-day-transparency.mjs scripts/finance-day-transparency-manifest.json
          git commit -m "Polish finance day and cash history UX"
          git push origin HEAD:fix/finance-day-human-polish-20260916
