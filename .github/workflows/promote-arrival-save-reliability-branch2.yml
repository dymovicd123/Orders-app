name: Promote arrival save reliability to branch2

on:
  push:
    branches:
      - branch2
    paths:
      - .github/workflows/promote-arrival-save-reliability-branch2.yml

permissions:
  contents: write

jobs:
  verify-and-promote:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: branch2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - run: npm ci
      - name: Verify exact baseline and copy tested hotfix
        shell: bash
        run: |
          set -euo pipefail
          BASE=b5566176b5159fc5b1c24c1930095879c3ea5552
          SAFE=6abec7b1f8c651296f3a59772d0d759084df481d
          git fetch origin main safe/arrival-save-reliability-20260829
          existing=(
            package.json
            scripts/test-step1906a-worker-modularization.mjs
            src/App.tsx
            worker/domains/inventory-movement.ts
          )
          added=(
            scripts/arrival-save-reliability-worker-manifest.json
            scripts/test-arrival-materialization-reliability.mjs
            scripts/test-arrival-save-reliability.mjs
          )
          for path in "${existing[@]}"; do
            current=$(git rev-parse "HEAD:${path}")
            expected=$(git rev-parse "${BASE}:${path}")
            if [ "$current" != "$expected" ]; then
              echo "Baseline mismatch: ${path} ${current} != ${expected}"
              exit 1
            fi
          done
          for path in "${added[@]}"; do
            if git cat-file -e "HEAD:${path}" 2>/dev/null; then
              echo "Expected new hotfix file already exists: ${path}"
              exit 1
            fi
          done
          expected_names=$(printf '%s\n' \
            package.json \
            scripts/arrival-save-reliability-worker-manifest.json \
            scripts/test-arrival-materialization-reliability.mjs \
            scripts/test-arrival-save-reliability.mjs \
            scripts/test-step1906a-worker-modularization.mjs \
            src/App.tsx \
            worker/domains/inventory-movement.ts | sort)
          actual_names=$(git diff --name-only "$BASE" "$SAFE" | sort)
          if [ "$actual_names" != "$expected_names" ]; then
            echo 'Safe hotfix changed an unexpected file set:'
            printf '%s\n' "$actual_names"
            exit 1
          fi
          git checkout "$SAFE" -- \
            package.json \
            scripts/arrival-save-reliability-worker-manifest.json \
            scripts/test-arrival-materialization-reliability.mjs \
            scripts/test-arrival-save-reliability.mjs \
            scripts/test-step1906a-worker-modularization.mjs \
            src/App.tsx \
            worker/domains/inventory-movement.ts
      - name: Full release gate
        run: npm run release:check
      - name: Commit verified branch2 hotfix
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/promote-arrival-save-reliability-branch2.yml
          changed=$(git status --short | sed -E 's/^...//' | sort)
          expected=$(printf '%s\n' \
            .github/workflows/promote-arrival-save-reliability-branch2.yml \
            package.json \
            scripts/arrival-save-reliability-worker-manifest.json \
            scripts/test-arrival-materialization-reliability.mjs \
            scripts/test-arrival-save-reliability.mjs \
            scripts/test-step1906a-worker-modularization.mjs \
            src/App.tsx \
            worker/domains/inventory-movement.ts | sort)
          if [ "$changed" != "$expected" ]; then
            echo 'Unexpected promotion working tree:'
            git status --short
            exit 1
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Fix arrival save reliability'
          git push origin HEAD:branch2
