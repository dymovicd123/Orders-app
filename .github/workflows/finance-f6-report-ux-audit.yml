name: Finance F6 adjacent report UX audit

on:
  push:
    branches:
      - finance-f6-release-audit

permissions:
  contents: write

jobs:
  audit-and-commit:
    if: ${{ !contains(github.event.head_commit.message, '[finance-f6-report-verified]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Apply adjacent report semantics patch
        run: python3 scripts/tmp-finance-f6-report-ux.py
      - name: Full release gate
        run: npm run release:check
      - name: Commit verified adjacent report semantics
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            worker/domains/finance-reports.ts \
            src/features/renderers/FinanceReportContentRenderer.tsx \
            src/app/types.ts \
            scripts/finance-f6-report-semantics-worker-manifest.json \
            scripts/test-finance-f6-release-audit.mjs \
            scripts/test-step1906a-worker-modularization.mjs \
            scripts/release-check.mjs
          git commit -m "Clarify Finance report cohort semantics [finance-f6-report-verified]"
          git push origin HEAD:finance-f6-release-audit
