-- This exact SELECT-only preflight lives on audit/branch2-0068-preflight-20260916.
-- The schema-sync workflow does not rely on this carrier file; retained only to make the ops branch self-contained.
SELECT 1;
